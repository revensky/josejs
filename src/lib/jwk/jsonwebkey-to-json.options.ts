/**
 * JSON Web Key **toJSON()** Options.
 */
export interface JsonWebKeyToJSONOptions {
  /**
   * Indicates that only the Public JSON Web Key Parameters will be exported.
   *
   * @default true
   */
  readonly exportPublicKeyOnly?: boolean;
}
